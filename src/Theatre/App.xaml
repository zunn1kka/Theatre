<Application x:Class="Theatre.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Theatre.Pages.Converters"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BooleanToInverseBooleanConverter" />
            <ResourceDictionary.MergedDictionaries>
                <!-- 1. Цветовая палитра -->
                <ResourceDictionary>
                    <!-- Основные цвета -->
                    <Color x:Key="PrimaryColor">#2C3E50</Color>
                    <Color x:Key="PrimaryLightColor">#34495E</Color>
                    <Color x:Key="PrimaryDarkColor">#1A252F</Color>
                    <Color x:Key="AccentColor">#E74C3C</Color>
                    <Color x:Key="AccentLightColor">#EC7063</Color>
                    <Color x:Key="SuccessColor">#2ECC71</Color>

                    <!-- Текст -->
                    <Color x:Key="TextColor">#333333</Color>
                    <Color x:Key="TextLightColor">#7F8C8D</Color>
                    <Color x:Key="TextOnPrimaryColor">#FFFFFF</Color>

                    <!-- Фоны -->
                    <Color x:Key="BackgroundColor">#F5F7FA</Color>
                    <Color x:Key="CardColor">#FFFFFF</Color>

                    <!-- Кисти -->
                    <SolidColorBrush x:Key="ColorPrimary" Color="{StaticResource PrimaryColor}"/>
                    <SolidColorBrush x:Key="ColorPrimaryLight" Color="{StaticResource PrimaryLightColor}"/>
                    <SolidColorBrush x:Key="ColorPrimaryDark" Color="{StaticResource PrimaryDarkColor}"/>
                    <SolidColorBrush x:Key="ColorAccent" Color="{StaticResource AccentColor}"/>
                    <SolidColorBrush x:Key="ColorAccentLight" Color="{StaticResource AccentLightColor}"/>
                    <SolidColorBrush x:Key="ColorSuccess" Color="{StaticResource SuccessColor}"/>
                    <SolidColorBrush x:Key="ColorText" Color="{StaticResource TextColor}"/>
                    <SolidColorBrush x:Key="ColorTextLight" Color="{StaticResource TextLightColor}"/>
                    <SolidColorBrush x:Key="ColorTextOnPrimary" Color="{StaticResource TextOnPrimaryColor}"/>
                    <SolidColorBrush x:Key="ColorBackground" Color="{StaticResource BackgroundColor}"/>
                    <SolidColorBrush x:Key="ColorCard" Color="{StaticResource CardColor}"/>
                    <SolidColorBrush x:Key="AvailableSeatBrush" Color="#4CAF50"/>
                    <SolidColorBrush x:Key="BookedSeatBrush" Color="#F44336"/>
                    <SolidColorBrush x:Key="LightBackgroundBrush" Color="#FAFAFA"/>
                    <!-- Эффекты -->
                    <DropShadowEffect x:Key="CardShadow" BlurRadius="10" ShadowDepth="2" Opacity="0.1" Color="Black"/>
                    <DropShadowEffect x:Key="FloatingShadow" BlurRadius="15" ShadowDepth="4" Opacity="0.2" Color="Black"/>
                    <DropShadowEffect x:Key="ButtonShadow" BlurRadius="5" ShadowDepth="1" Opacity="0.1" Color="Black"/>
                </ResourceDictionary>

                <!-- 2. Базовые стили элементов -->
                <ResourceDictionary>
                    <!-- Текстовые стили -->
                    <Style x:Key="BaseTextBlockStyle" TargetType="TextBlock">
                        <Setter Property="FontFamily" Value="Segoe UI"/>
                        <Setter Property="Foreground" Value="{StaticResource ColorText}"/>
                    </Style>

                    <Style x:Key="ModernTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}">
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Margin" Value="0,5,0,5"/>
                    </Style>

                    <Style x:Key="FormTitleTextStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}">
                        <Setter Property="FontSize" Value="24"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Foreground" Value="{StaticResource ColorPrimary}"/>
                        <Setter Property="HorizontalAlignment" Value="Center"/>
                        <Setter Property="Margin" Value="0,0,0,20"/>
                    </Style>

                    <Style x:Key="InputLabelTextStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}">
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Margin" Value="0,0,0,5"/>
                    </Style>

                    <Style x:Key="CardTitleTextStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}">
                        <Setter Property="FontSize" Value="16"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="TextAlignment" Value="Center"/>
                        <Setter Property="Margin" Value="0,0,0,2"/>
                    </Style>

                    <Style x:Key="CardSubtitleTextStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}">
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="Foreground" Value="{StaticResource ColorTextLight}"/>
                        <Setter Property="TextAlignment" Value="Center"/>
                        <Setter Property="Margin" Value="0,0,0,5"/>
                    </Style>

                    <Style x:Key="CardRatingTextStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}">
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Foreground" Value="{StaticResource ColorAccent}"/>
                    </Style>

                    <!-- Базовый стиль для текстовых полей -->
                    <Style x:Key="BaseTextBoxStyle" TargetType="TextBox">
                        <Setter Property="Background" Value="{StaticResource ColorCard}"/>
                        <Setter Property="Foreground" Value="{StaticResource ColorText}"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="FontFamily" Value="Segoe UI"/>
                        <Setter Property="Padding" Value="6,4"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        <Setter Property="MinHeight" Value="28"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TextBox">
                                    <Border x:Name="border" 
                        Background="{TemplateBinding Background}"
                        CornerRadius="4"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        SnapsToDevicePixels="True">
                                        <ScrollViewer x:Name="PART_ContentHost" 
                                  Margin="{TemplateBinding Padding}"
                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource ColorPrimaryLight}"/>
                                        </Trigger>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource ColorAccent}"/>
                                            <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <!-- Cтили текстовых полей с иконкой -->
                    <Style x:Key="IconTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource BaseTextBoxStyle}">
                        <Setter Property="Padding" Value="10,10,40,10"/>
                        <Setter Property="Background">
                            <Setter.Value>
                                <VisualBrush Stretch="None" AlignmentX="Right" AlignmentY="Center">
                                    <VisualBrush.Visual>
                                        <Image Source="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"
                           Width="20"
                           Height="20"
                           Margin="0,0,10,0"/>
                                    </VisualBrush.Visual>
                                </VisualBrush>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <!-- Поле ввода с валидацией -->
                    <Style x:Key="ValidatableTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource BaseTextBoxStyle}">
                        <Setter Property="Validation.ErrorTemplate">
                            <Setter.Value>
                                <ControlTemplate>
                                    <StackPanel>
                                        <!-- Основное поле ввода с красной рамкой при ошибке -->
                                        <Border BorderBrush="Red" BorderThickness="1" CornerRadius="4">
                                            <AdornedElementPlaceholder/>
                                        </Border>
                                        <!-- Текст ошибки -->
                                        <TextBlock Text="{Binding [0].ErrorContent}" 
                               Foreground="Red" 
                               Margin="5,2,0,0"
                               FontSize="12"/>
                                    </StackPanel>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="Validation.HasError" Value="true">
                                <Setter Property="ToolTip" 
                    Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <!-- Стиль для PasswordBox -->
                    <Style x:Key="ModernPasswordBoxBaseStyle" TargetType="PasswordBox">
                        <Setter Property="Background" Value="{StaticResource ColorCard}"/>
                        <Setter Property="Foreground" Value="{StaticResource ColorText}"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="FontFamily" Value="Segoe UI"/>
                        <Setter Property="Padding" Value="6,4"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        <Setter Property="MinHeight" Value="28"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="PasswordBox">
                                    <Border x:Name="border" 
                        Background="{TemplateBinding Background}"
                        CornerRadius="4"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        SnapsToDevicePixels="True">
                                        <ScrollViewer x:Name="PART_ContentHost" 
                                  Margin="{TemplateBinding Padding}"
                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource ColorPrimaryLight}"/>
                                        </Trigger>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource ColorAccent}"/>
                                            <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <!-- Базовый стиль для кнопок -->
                    <Style x:Key="BaseButtonStyle" TargetType="Button">
                        <Setter Property="Background" Value="{StaticResource ColorPrimary}"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="FontFamily" Value="Segoe UI"/>
                        <Setter Property="Padding" Value="12,6"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="6"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            BorderBrush="{TemplateBinding BorderBrush}">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource ColorPrimaryLight}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{StaticResource ColorPrimaryDark}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <Style x:Key="ModernButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
                        <Setter Property="Effect" Value="{StaticResource ButtonShadow}"/>
                        <Setter Property="Margin" Value="5"/>
                        <Setter Property="Height" Value="30"/>
                    </Style>

                    <Style x:Key="SeatButtonStyle" TargetType="Button">
                        <Setter Property="Width" Value="40"/>
                        <Setter Property="Height" Value="40"/>
                        <Setter Property="Margin" Value="5"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Background" Value="{StaticResource AvailableSeatBrush}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Border" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="#444" 
                            BorderThickness="1"
                            CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="BorderBrush" Value="#0078D7"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsBooked}" Value="True">
                                <Setter Property="Background" Value="{StaticResource BookedSeatBrush}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>

                    <Style x:Key="TextButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource ColorPrimary}"/>
                        <Setter Property="Padding" Value="8,4"/>
                    </Style>

                    <Style x:Key="LoginButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
                        <Setter Property="Background" Value="{StaticResource ColorAccent}"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontSize" Value="16"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="15,10"/>
                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        <Setter Property="Effect" Value="{StaticResource FloatingShadow}"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource ColorAccentLight}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <Style x:Key="FloatingActionButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
                        <Setter Property="Background" Value="{StaticResource ColorAccent}"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="20,10"/>
                        <Setter Property="Effect" Value="{StaticResource FloatingShadow}"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource ColorAccentLight}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <!-- Стиль для карточек спектаклей -->
                    <Style x:Key="ImageCardButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="8"
                                            BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.3" Color="Black"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <!-- Стиль для гиперссылок -->
                    <Style x:Key="ModernHyperlinkStyle" TargetType="Hyperlink">
                        <Setter Property="Foreground" Value="{StaticResource ColorAccent}"/>
                        <Setter Property="TextDecorations" Value="None"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource ColorAccentLight}"/>
                                <Setter Property="TextDecorations" Value="Underline"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <!-- Стиль для ComboBox -->
                    <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
                        <Setter Property="Background" Value="{StaticResource ColorCard}"/>
                        <Setter Property="Foreground" Value="{StaticResource ColorText}"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="FontFamily" Value="Segoe UI"/>
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        <Setter Property="MinHeight" Value="36"/>
                        <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBox">
                                    <Grid>
                                        <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter x:Name="contentPresenter"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                            Margin="{TemplateBinding Padding}"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left"/>
                                                <Path x:Name="Arrow"
                                  Grid.Column="1"
                                  Fill="{StaticResource ColorTextLight}"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                        <Popup x:Name="PART_Popup"
                          Placement="Bottom"
                          IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                          AllowsTransparency="True"
                          Focusable="False"
                          PopupAnimation="Slide">
                                            <Grid MinWidth="{TemplateBinding ActualWidth}"
                              MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                                <Border x:Name="PopupBorder"
                                    Background="{StaticResource ColorCard}"
                                    CornerRadius="6"
                                    BorderThickness="1"
                                    BorderBrush="{StaticResource ColorPrimaryLight}"
                                    Effect="{StaticResource FloatingShadow}">
                                                    <ScrollViewer>
                                                        <ItemsPresenter/>
                                                    </ScrollViewer>
                                                </Border>
                                            </Grid>
                                        </Popup>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource ColorPrimaryLight}"/>
                                        </Trigger>
                                        <Trigger Property="IsDropDownOpen" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource ColorAccent}"/>
                                            <Setter TargetName="Arrow" Property="Data" Value="M 0 4 L 4 0 L 8 4 Z"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <!-- Стиль для контейнера формы -->
                    <Style x:Key="FormContainerStyle" TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource ColorCard}"/>
                        <Setter Property="CornerRadius" Value="10"/>
                        <Setter Property="Padding" Value="40"/>
                        <Setter Property="BorderBrush" Value="{StaticResource ColorAccent}"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
                    </Style>

                    <!-- Стиль для страницы капчи -->
                    <Style x:Key="CaptchaPageStyle" TargetType="Page">
                        <Setter Property="Background" Value="{StaticResource ColorBackground}"/>
                    </Style>

                    <!-- Стиль для элемента места в спектакле -->
                    <Style x:Key="SeatItemStyle" TargetType="Border">
                        <Setter Property="BorderBrush" Value="Gray"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Margin" Value="5"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="Background" Value="{StaticResource ColorCard}"/>
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                    <DropShadowEffect x:Key="ShadowEffect" 
                 BlurRadius="10" 
                 ShadowDepth="3" 
                 Color="#33000000" 
                 Opacity="0.6"/>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>